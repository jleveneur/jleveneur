http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.jleveneur.com`)"
      entryPoints:
        - websecure
      service: api@internal
      middlewares:
        - secure-headers
        - dashboard-auth
      tls:
        certResolver: letsencrypt
